<title>Private Events Homepage</title>
<h1>Welcome to Private Events<% if logged_in? %>, <%= @current_user.name %><% end %></h1>
<div>

    <%unless logged_in? %>
        <p>Want to join? <%= link_to 'Sign up', signup_path %></p>
        <p>Already a member? <%= link_to 'Login', login_path %></p>
    <%else%>

        <div>
            <h3>You are logged in!</h3>
        </div>
    <% end %>
</div>            